#include <stdio.h>

/**
 * main - print fibonacci
 * Description: prints fibonacci numbers
 * Return: always 0 when success
 */
int main(void)
{
long int q, w, e, r;
q = 1;
w = 2;
e = 3;
printf("%ld, ", q);
printf("%ld, ", w);
for (r = 3; r < 50; r++)
{
printf("%ld, ", e);
q = b;
w = c;
e = q + w;
}
printf("%ld\n", e);
return (0);
}
