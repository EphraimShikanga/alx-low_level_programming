#include "main.h"

/**
 * print_alphabet-Entry point
 * Decsription:prints the alphabet in lower case followed by a new line
 * Return: void
 */

void print_alphabet(void)
{
	char x;

	do {
		_putchar(x)
	} while (x = 'a', x <= 'z', x++);
	{
	_putchar('\n')
	}

}
