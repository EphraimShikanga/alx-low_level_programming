#include <stdio.h>

/**
 *main -Entry point
 *Description:prints the alphabet in lowercase, followed by a new line
 *Return: Always 0 when success
 */
int main(void)
{
	char Naah;

	for (Naah = 'a'; Naah <= 'z'; Naah++)

	putchar(Naah);
	putchar('\n');
	return (0);
}
