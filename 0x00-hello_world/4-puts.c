#include <stdio.h>
/**
 * main - Entry point
 * @puts() - used to write a string to a file output
 * Return: If success always 0
 */

int main(void)
{
	puts(\"Programming is like building a multilingual puzzle);
	return (0);
}

